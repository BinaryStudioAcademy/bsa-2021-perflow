import { SemanticMessageDirective } from './semantic-message.directive';

describe('SemanticMessageDirective', () => {
  it('should create an instance', () => {
    const directive = new SemanticMessageDirective();
    expect(directive).toBeTruthy();
  });
});
