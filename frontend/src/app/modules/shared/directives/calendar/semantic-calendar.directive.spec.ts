import { SemanticCalendarDirective } from './semantic-calendar.directive';

describe('SemanticCalendarDirective', () => {
  it('should create an instance', () => {
    const directive = new SemanticCalendarDirective();
    expect(directive).toBeTruthy();
  });
});
