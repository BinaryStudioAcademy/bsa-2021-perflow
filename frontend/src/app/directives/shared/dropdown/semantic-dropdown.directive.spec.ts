import { SemanticDropdownDirective } from './semantic-dropdown.directive';

describe('SemanticDropdownDirective', () => {
  it('should create an instance', () => {
    const directive = new SemanticDropdownDirective();
    expect(directive).toBeTruthy();
  });
});
