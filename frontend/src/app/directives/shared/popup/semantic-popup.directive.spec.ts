import { SemanticPopupDirective } from './semantic-popup.directive';

describe('SemanticPopupDirective', () => {
  it('should create an instance', () => {
    const directive = new SemanticPopupDirective();
    expect(directive).toBeTruthy();
  });
});
